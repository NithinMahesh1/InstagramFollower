package com.InstagramFollower;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class InstagramFollowerApplication {

//	TODO solution to dealing with captcha - build out the actual application and put script on hold
	// TODO - Create the client side and have a screenshot created with an image added to the view
	// TODO - In here we will have the user input the time value displayed which will be passed back to the controller then model and added to the script

	public static void main(String[] args) {
		SpringApplication.run(InstagramFollowerApplication.class, args);
	}

}
